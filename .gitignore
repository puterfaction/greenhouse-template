# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Worktrees live here; don't commit their contents
.claude/worktrees/

# Local-only Claude Code settings
.claude/settings.local.json

# Tend hygiene logs (reports are kept, the raw launchd log is not)
tend-reports/launchd.log

# Handover files live outside the repo, but ignore if symlinked in
handover.md

# Subconscious runtime artifacts (rebuild with: python3 tools/subconscious.py --index)
tools/subconscious_index.json
