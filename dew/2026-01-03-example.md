---
date: 2026-01-03
pushed: true
---
morning — search is nearly there, one decision needed.

Alive: the search rewrite on wt/search-index — index builds in ~2s, ranking is the open problem (title matches score same as body matches).
Up next: title-weight scoring, first task of the next session — decided last night rather than tuning tired.
Small ask: check the recipe detail page's December flexbox fix before debugging the mobile overflow — the agent suspects it's the same bug.

> **Fictional worked example** (continues the "Pantry" storyline from `now.md` and the
> diary). Delete this file and `index.json`'s entry for it once your first real dew
> lands. Note the shape: greeting, labeled bullets, scaled to the day — this fictional
> day has one thread, one queued item, one ask.
